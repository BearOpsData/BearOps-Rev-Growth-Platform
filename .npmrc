# Security: Audit on install
audit=true
audit-level=moderate

# Security: Use package-lock.json
package-lock=true

# Security: Disable lifecycle scripts from untrusted packages by default
# Run needed scripts explicitly (e.g., npx prisma generate)
ignore-scripts=true

